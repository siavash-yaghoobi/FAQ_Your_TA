<%= simple_form_for([ @user, @duck]) do |form| %>
  <%= form.input :name %>
  <%= form.input :category %>
  <%= form.input :description %>
  <%= form.button :submit %>
<% end %>

<%= link_to('Back to home', root_path) %>
