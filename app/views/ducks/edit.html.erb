<div class="container">
  <%= simple_form_for([ @user, @duck]) do |form| %>
    <%= form.input :name %>
    <%= form.input :category %>
    <%= form.input :description %>
    <%= form.button :submit, class: "btn btn-success"%>
  <% end %>
</div>
